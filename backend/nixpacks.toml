# Nixpacks Configuration for Render
[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build", "npm run prisma:generate"]

[start]
cmd = "npm run start:prod"
