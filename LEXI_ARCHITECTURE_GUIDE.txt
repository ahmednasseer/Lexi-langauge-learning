===============================================================================
                            HISTORICAL REFERENCE
===============================================================================
Status: SUPERSEDED — DO NOT USE AS CURRENT STATE REFERENCE
Current State Audit: See LEXI_CURRENT_STATE_AUDIT.md
===============================================================================

# LEXI ARCHITECTURE GUIDE

## 1. VISION

### ظ‡ط¯ظپ ط§ظ„ظ…ط´ط±ظˆط¹
طھط·ط¨ظٹظ‚ طھط¹ظ„ظٹظ… ط§ظ„ظ„ط؛ط© ط§ظ„ط£ظ„ظ…ط§ظ†ظٹط© ط¨ط§ظ„ط°ظƒط§ط، ط§ظ„ط§طµط·ظ†ط§ط¹ظٹ ظٹظˆظپط± طھط¬ط±ط¨ط© طھط¹ظ„ظ… ط´ط®طµظٹط© ظˆظ…ظ…طھط¹ط©.

### ط§ظ„ظ…ط³طھط®ط¯ظ… ط§ظ„ظ…ط³طھظ‡ط¯ظپ
- ط§ظ„ظ…ط¨طھط¯ط¦ظˆظ† ظپظٹ طھط¹ظ„ظ… ط§ظ„ظ„ط؛ط© ط§ظ„ط£ظ„ظ…ط§ظ†ظٹط© (A1-B1)
- ط§ظ„ط·ظ„ط§ط¨ ط§ظ„ط°ظٹظ† ظٹط­ط¶ط±ظˆظ† ظ„ط§ظ…طھط­ط§ظ†ط§طھ ط؛ظˆطھظ‡
- ط§ظ„ظ…ط؛طھط±ط¨ظˆظ† ظپظٹ ط£ظ„ظ…ط§ظ†ظٹط§

### ط§ظ„ظ…ط¨ط§ط¯ط¦ ط§ظ„ط£ط³ط§ط³ظٹط©
- **Simplicity First**: ط¨ط³ط§ط·ط© ظ‚ط¨ظ„ ط§ظ„طھط¹ظ‚ظٹط¯
- **User First**: ط§ظ„ظ…ط³طھط®ط¯ظ… ط£ظˆظ„ط§ظ‹
- **Incremental Development**: طھط·ظˆظٹط± طھط¯ط±ظٹط¬ظٹ
- **Test Before Ship**: ط§ط®طھط¨ط§ط± ظ‚ط¨ظ„ ط¥ط·ظ„ط§ظ‚
- **No Mock in Production**: ظ…ظ…ظ†ظˆط¹ ط§ظ„ط¨ظٹط§ظ†ط§طھ ط§ظ„ظˆظ‡ظ…ظٹط© ظپظٹ ط§ظ„ط¥ظ†طھط§ط¬

---

## 2. ARCHITECTURE

### Pattern: Feature First + Clean Architecture

```
lib/
â”œâ”€â”€ core/
â”‚   â”œâ”€â”€ di/                    # Dependency Injection
â”‚   â”œâ”€â”€ error/                 # Error Handling
â”‚   â”œâ”€â”€ logger/                # Logging
â”‚   â”œâ”€â”€ network/               # Network Layer
â”‚   â”œâ”€â”€ state/                 # Base State Classes
â”‚   â”œâ”€â”€ storage/               # Local Storage
â”‚   â””â”€â”€ utils/                 # Utilities
â”œâ”€â”€ data/
â”‚   â”œâ”€â”€ datasources/
â”‚   â”‚   â”œâ”€â”€ local/             # Local Data Sources
â”‚   â”‚   â””â”€â”€ remote/            # Remote Data Sources
â”‚   â”œâ”€â”€ models/                # Data Models (DTOs)
â”‚   â””â”€â”€ repositories/          # Repository Implementations
â”œâ”€â”€ domain/
â”‚   â”œâ”€â”€ entities/              # Business Entities
â”‚   â”œâ”€â”€ repositories/          # Repository Interfaces
â”‚   â””â”€â”€ usecases/              # Business Logic
â”œâ”€â”€ presentation/
â”‚   â”œâ”€â”€ bloc/                  # Blocs
â”‚   â”œâ”€â”€ pages/                 # Screens
â”‚   â”œâ”€â”€ widgets/               # Reusable Widgets
â”‚   â””â”€â”€ theme/                 # Theme
â””â”€â”€ main.dart

```

### ط·ط¨ظ‚ط§طھ ط§ظ„ظ…ط´ط±ظˆط¹

| Layer | Responsibility |
|-------|----------------|
| Presentation | UI, Blocs, Widgets |
| Domain | Entities, UseCases, Repository Interfaces |
| Data | Models, DataSources, Repository Implementations |
| Core | DI, Logger, Error, Network |

### ظ‚ظˆط§ط¹ط¯ ط§ظ„ظ†ظ‚ظ„ ط¨ظٹظ† ط§ظ„ط·ط¨ظ‚ط§طھ
- **ظ…ط³ظ…ظˆط­**: Domain â†گ Data â†گ Core
- **ظ…ط³ظ…ظˆط­**: Presentation â†گ Domain â†گ Data
- **ظ…ظ…ظ†ظˆط¹**: Data â†گ Presentation
- **ظ…ظ…ظ†ظˆط¹**: Core â†گ Domain
- **ظ…ظ…ظ†ظˆط¹**: ط£ظٹ Model ظٹط¹ط¨ط± ط·ط¨ظ‚ط© ط¨ط¯ظˆظ† طھط­ظˆظٹظ„

---

## 3. STATE MANAGEMENT

### System: flutter_bloc (Bloc/Cubit)

#### ط§ظ„ظ‚ظˆط§ط¹ط¯:
- ط§ط³طھط®ط¯ط§ظ… **Cubit** ظ„ظ„ط­ط§ظ„ط§طھ ط§ظ„ط¨ط³ظٹط·ط©
- ط§ط³طھط®ط¯ط§ظ… **Bloc** ظ„ظ„ط­ط§ظ„ط§طھ ط§ظ„ظ…ط¹ظ‚ط¯ط© ظ…ط¹ Events
- ظƒظ„ Screen ظ„ظ‡ط§ Bloc/Cubit ط®ط§طµ
- ظ„ط§ ظٹظˆط¬ط¯ Bloc ط¹ط§ظ… ظ„ط£ظƒط«ط± ظ…ظ† 3 Screens

#### ط¯ظˆط±ط© ط§ظ„ط­ظٹط§ط©:
- **ط§ظ„ط¥ظ†ط´ط§ط،**: ط¹ظ†ط¯ ظپطھط­ ط§ظ„ط´ط§ط´ط© (BlocProvider create)
- **ط§ظ„طھط®ظ„طµ**: ط¹ظ†ط¯ ط¥ط؛ظ„ط§ظ‚ ط§ظ„ط´ط§ط´ط© (طھظ„ظ‚ط§ط¦ظٹ via BlocProvider)
- **ط§ظ„طھط¬ط¯ظٹط¯**: ط¹ظ†ط¯ ط§ظ„ط­ط§ط¬ط© (BlocProvider.value)

#### طھط­ط¯ظٹط« ط§ظ„ظˆط§ط¬ظ‡ط©:
- `emit(newState)` ظپظ‚ط·
- ظ„ط§ setState ط¯ط§ط®ظ„ BlocBuilder
- Equatable ظ„ط¬ظ…ظٹط¹ States

---

## 4. DEPENDENCY INJECTION

### System: get_it + injectable

#### ط§ظ„ط£ظ†ظˆط§ط¹:
| Type | Usage |
|------|-------|
| Singleton | Services, Repositories |
| Lazy Singleton | Database, Firebase instances |
| Factory | Blocs, UseCases |

#### ط§ظ„ظ‚ظˆط§ط¹ط¯:
- ط§ظ„طھط³ط¬ظٹظ„ ظپظٹ `injection_container.dart`
- ظ„ط§ new ظ…ط¨ط§ط´ط±ط© ط£ط¨ط¯ظ‹ط§
- ظƒظ„ ط´ظٹط، ط¹ط¨ط± GetIt.instance
- `@injectable` annotation ظ„ظƒظ„ ط§ظ„ظƒظ„ط§ط³ط§طھ

---

## 5. REPOSITORY PATTERN

### ط§ظ„ظ‡ظٹظƒظ„ ط§ظ„ط±ط³ظ…ظٹ:

```dart
// Interface (Domain Layer)
abstract class UserRepository {
  Future<User> getUser(String id);
  Future<void> updateUser(User user);
}

// Implementation (Data Layer)
class UserRepositoryImpl implements UserRepository {
  final UserRemoteDataSource _remote;
  final UserLocalDataSource _local;
  
  @override
  Future<User> getUser(String id) async {
    try {
      final user = await _remote.getUser(id);
      await _local.cacheUser(user);
      return user;
    } catch (e) {
      return _local.getCachedUser(id);
    }
  }
}
```

### ط§ظ„ظ…طµط§ط¯ط±:
- **Remote**: Firestore, Firebase Auth, APIs
- **Local**: SharedPreferences, Hive, SQLite

### ط§ظ„ظ‚ظˆط§ط¹ط¯:
- Repository ظٹط¨ط¯ط£ ط¨ظ€ Remote
- ظٹظپط´ظ„ â†’ ظٹط¹ظˆط¯ ط¥ظ„ظ‰ Local
- ظٹظ†ط¬ط­ â†’ ظٹط®ط²ظ† ظپظٹ Local

---

## 6. BACKEND RULES

### Firebase Collections:

```
users/{userId}                 # ط¨ظٹط§ظ†ط§طھ ط§ظ„ظ…ط³طھط®ط¯ظ…
users/{userId}/progress        # ط§ظ„طھظ‚ط¯ظ…
users/{userId}/inventory       # ط§ظ„ظ…ط®ط²ظˆظ†

curriculum/{level}/units/{unitId}/lessons/{lessonId}
questions/{level}/questions/{questionId}
audio/{audioId}
pronunciation/{pronunciationId}
speaking/{exerciseId}

posts/{postId}
posts/{postId}/comments/{commentId}
posts/{postId}/likes/{userId}

friends/{userId}/friends/{friendId}
messages/{conversationId}/messages/{messageId}

store/{itemId}
inventory/{userId}/items/{itemId}

achievements/{achievementId}
missions/{missionId}
streaks/{userId}
quests/{questId}
leaderboard/{period}

events/{eventId}
rooms/{roomId}

notifications/{userId}/notifications/{notifId}
```

### Security Rules:
```javascript
// Firestore
match /users/{userId} {
  allow read, write: if request.auth != null && request.auth.uid == userId;
}

match /posts/{postId} {
  allow read: if request.auth != null;
  allow write: if request.auth != null && request.auth.uid == resource.data.authorId;
}
```

---

## 7. UI RULES

### Design System:
- **Theme**: Dark/Light ظ…ط¹ Material 3
- **Typography**: Poppins (Google Fonts)
- **Colors**: ظ…ط¹ط±ظپط© ظپظٹ AppColors
- **Icons**: Material Icons

### ط§ظ„ط­ط§ظ„ط§طھ:
| State | Widget |
|-------|--------|
| Loading | Skeleton/CircularProgressIndicator |
| Empty | EmptyState ظ…ط¹ ط±ط³ط§ظ„ط© ظˆط¥ط¬ط±ط§ط، |
| Error | ErrorState ظ…ط¹ Retry |
| Success | ط§ظ„ظ…ط­طھظˆظ‰ ط§ظ„ظپط¹ظ„ظٹ |

### ط§ظ„ط£ظ†ظٹظ…ظٹط´ظ†:
- ط§ط³طھط®ط¯ط§ظ… flutter_animate
- Duration: 200-400ms
- ظ„ط§ ط£ظ†ظٹظ…ظٹط´ظ†ط§طھ ط«ظ‚ظٹظ„ط©

---

## 8. NAVIGATION RULES

### System: Named Routes

### Route Naming:
```
/auth/login
/auth/register
/auth/forgot-password
/home
/profile
/profile/edit
/lessons
/lessons/{lessonId}
/quiz/{quizId}
/store
/store/inventory
/community
/chat/{userId}
/settings
```

### Guards:
```dart
RouteGuard(
  requiresAuth: ['/home', '/profile', '/lessons'],
  requiresGuest: ['/auth/login', '/auth/register'],
);
```

---

## 9. ASSETS RULES

### ط§ظ„ظ‡ظٹظƒظ„:
```
assets/
â”œâ”€â”€ images/
â”‚   â”œâ”€â”€ logo.png
â”‚   â”œâ”€â”€ characters/
â”‚   â”œâ”€â”€ badges/
â”‚   â”œâ”€â”€ frames/
â”‚   â””â”€â”€ backgrounds/
â”œâ”€â”€ audio/
â”‚   â””â”€â”€ lessons/
â””â”€â”€ lottie/
```

### Naming:
- ط£ط³ظ…ط§ط، طµط؛ظٹط±ط© ظ…ط¹ underscore
- ظ…ط«ط§ظ„: `lexi_happy.png`, `badge_a1.png`

### Optimization:
- WebP ظ„ظ„طµظˆط±
- Compression: 80%
- Max size: 500KB per image

---

## 10. ERROR HANDLING

### ط§ظ„ط£ظ†ظˆط§ط¹:
```dart
abstract class Failure {}
class NetworkFailure extends Failure {}
class FirebaseFailure extends Failure {}
class ValidationFailure extends Failure {}
class UnknownFailure extends Failure {}
```

### ط§ظ„ظ‚ظˆط§ط¹ط¯:
- ظƒظ„ Repository ظٹط¹ظٹط¯ `Either<Failure, Success>`
- ظ„ط§ try-catch ظپظٹ UI
- Bloc ظٹظ„طھظ‚ط· ط§ظ„ط£ط®ط·ط§ط، ظˆظٹط­ظˆظ„ظ‡ط§ ظ„ظ€ State
- ط±ط³ط§ظ„ط© ط®ط·ط£ ظˆط§ط¶ط­ط© ظ„ظ„ظ…ط³طھط®ط¯ظ…

---

## 11. LOGGING

### ط§ظ„ظ‚ظˆط§ط¹ط¯:
| Level | Usage |
|-------|-------|
| Debug | Development only |
| Info | User actions, Navigation |
| Warning | Deprecated API, Slow operations |
| Error | Failures, Exceptions |

### ط§ظ„ط§ط³طھط®ط¯ط§ظ…:
```dart
AppLogger.d('Debug message');
AppLogger.i('User logged in: $userId');
AppLogger.w('Slow operation detected');
AppLogger.e('Error occurred', error, stackTrace);
```

---

## 12. PERFORMANCE RULES

### Pagination:
```dart
// Firestore
collection
  .orderBy('createdAt')
  .limit(20)
  .startAfterDocument(lastDocument);
```

### Lazy Loading:
- ListView.builder ط¯ط§ط¦ظ…ظ‹ط§
- ظ„ط§ shrinkWrap ط¥ظ„ط§ ظ„ظ„ط¶ط±ظˆط±ط©
- addAutomaticKeepAlives: false

### Image Cache:
- cached_network_image package
- Max age: 7 days
- Max size: 100MB

### Rebuild Rules:
- const constructors
- RepaintBoundary ظ„ظ„ظˆط¯ط¬ط§طھ ط§ظ„ظ…طھط­ط±ظƒط©
- ظ„ط§ setState ظپظٹ build

---

## 13. SECURITY RULES

### Authentication:
- Firebase Auth ط¥ظ„ط²ط§ظ…ظٹ
- Token refresh طھظ„ظ‚ط§ط¦ظٹ
- Session timeout: 30 days

### Input Validation:
```dart
// ط¬ظ…ظٹط¹ ط§ظ„ظ…ط¯ط®ظ„ط§طھ طھظڈط­ظ‚ظ‚
final emailValidator = EmailValidator();
final passwordValidator = PasswordValidator(minLength: 8);
```

### Secure Storage:
- flutter_secure_storage ظ„ظ„ظ€ Tokens
- ظ„ط§ طھط®ط²ظٹظ† ط¨ظٹط§ظ†ط§طھ ط­ط³ط§ط³ط© ظپظٹ SharedPreferences

### API Keys:
- ظپظٹ ظ…طھط؛ظٹط±ط§طھ ط§ظ„ط¨ظٹط¦ط© ظپظ‚ط·
- ظ„ط§ ظپظٹ ط§ظ„ظƒظˆط¯ ط£ط¨ط¯ظ‹ط§
- --dart-define ظ„ظ„ظ‚ظٹظ… ط§ظ„ظ…ط®طھظ„ظپط©

---

## 14. TESTING STRATEGY

### ط§ظ„ط­ط¯ظˆط¯ ط§ظ„ط£ط¯ظ†ظ‰:

| Type | Coverage | Required Before Merge |
|------|----------|----------------------|
| Unit Tests | 70% | Yes |
| Widget Tests | 50% | Yes |
| Integration Tests | Critical paths | Yes |

### Unit Tests:
```dart
// ظ„ظƒظ„ UseCase
// ظ„ظƒظ„ Repository
// ظ„ظƒظ„ Bloc
test('should return user when data exists', () async {
  // arrange
  // act
  // assert
});
```

### Widget Tests:
```dart
// ظ„ظƒظ„ Screen ط±ط¦ظٹط³ظٹط©
testWidgets('should show loading initially', (tester) async {
  // pumpWidget
  // expect CircularProgressIndicator
});
```

---

## 15. CODING STANDARDS

### Naming:
| Type | Convention | Example |
|------|------------|---------|
| Class | PascalCase | UserRepository |
| Method | camelCase | getUser |
| Variable | camelCase | userName |
| Constant | UPPER_SNAKE_CASE | MAX_RETRIES |
| File | snake_case | user_repository.dart |
| Folder | snake_case | user_management |

### ط§ظ„ظ‚ظˆط§ط¹ط¯:
- ظƒظ„ Class ظپظٹ ظ…ظ„ظپ ظ…ظ†ظپطµظ„
- ظ„ط§ ظ…ظ„ظپط§طھ طھطھط¬ط§ظˆط² 300 ط³ط·ط±
- ظƒظ„ Method ظ„ط§ ظٹطھط¬ط§ظˆط² 30 ط³ط·ط±
- طھط¹ظ„ظٹظ‚ ظ„ظƒظ„ Public API

### Comments:
```dart
/// Fetches user by ID
/// Throws [UserNotFoundException] if user doesn't exist
Future<User> getUser(String id) async {
  // implementation
}
```

---

## 16. GIT WORKFLOW

### Branch Strategy:
- `main`: Production-ready
- `develop`: Integration
- `feature/TASK-xxx`: New features
- `fix/TASK-xxx`: Bug fixes

### Commit Convention:
```
feat(TASK-001): add user authentication
fix(TASK-002): resolve login crash
refactor(TASK-003): optimize repository pattern
test(TASK-004): add user bloc tests
docs(TASK-005): update architecture guide
```

### Pull Request:
- Minimum 1 reviewer
- All tests pass
- No merge conflicts
- Code reviewed

---

## 17. DEFINITION OF DONE

ظ„ط§ طھط¹طھط¨ط± ط£ظٹ Task ظ…ظ†طھظ‡ظٹط© ط¥ظ„ط§ ط¥ط°ط§:

- [ ] طھط¹ظ…ظ„ ط¨ط§ظ„ظƒط§ظ…ظ„
- [ ] ط¬ظ…ظٹط¹ ط§ظ„طھط¨ط¹ظٹط§طھ ظ…ظƒطھظ…ظ„ط©
- [ ] ظ„ط§ ظٹظˆط¬ط¯ TODO
- [ ] ظ„ط§ ظٹظˆط¬ط¯ Mock Data
- [ ] ظ„ط§ ظٹظˆط¬ط¯ Dead Code
- [ ] ظ„ط§ ظٹظˆط¬ط¯ Warning
- [ ] ظ„ط§ ظٹظˆط¬ط¯ Error
- [ ] Unit Tests ظ…ظƒطھظˆط¨ط©
- [ ] Widget Tests ظ…ظƒطھظˆط¨ط© (ظ„ظ„ط´ط§ط´ط§طھ)
- [ ] طھظ…طھ ظ…ط±ط§ط¬ط¹ط© ط§ظ„ظƒظˆط¯
- [ ] طھظ… ط§ط®طھط¨ط§ط±ظ‡ط§ ظٹط¯ظˆظٹظ‹ط§
- [ ] طھظ… طھط­ط¯ظٹط« ط§ظ„ظˆط«ط§ط¦ظ‚ ط¥ط°ط§ ظ„ط²ظ… ط§ظ„ط£ظ…ط±
- [ ] PR approved
- [ ] Merged to develop

---

## CONFLICTS RESOLVED

| Conflict | Resolution | Reason |
|----------|------------|--------|
| State Management | Bloc over Riverpod | Better tooling, clearer separation |
| DI | GetIt over Riverpod DI | Simpler, faster compilation |
| Storage | Hive over SQLite | Faster, simpler API |
| Navigation | Named Routes over GoRouter | Simpler, sufficient for current needs |

---

# END OF ARCHITECTURE GUIDE

